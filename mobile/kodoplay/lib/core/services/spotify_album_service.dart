class SpotifyAlbumService{
  /// get list Abum from Spotify API
  // Future<List<Album>> getAlbums() async {
  //   final response = await http.get('https://api.spotify.com/v1/albums');
  //   if (response.statusCode == 200) {
  //     final parsed = json.decode(response.body);
  //     return parsed['albums'].map<Album>((json) => Album.fromJson(json)).toList();
  //   } else {
  //     throw Exception('Error while fetching albums');
  //   }
  // }
}